# Apps Script: private config + clasp link stay out of this public repo
apps-script/Config.js
apps-script/.clasp.json
# older unused prototype script (not what the app talks to)
apps-script-b/
# route-map tools: big working files (master.csv is ~40 MB) and the venv stay local
tools/route-maps/work/
tools/route-maps/venv/
# source documents (the run route-map PDFs and zips) are large and live only on this Mac
/*.pdf
/*.zip
tools/parcels/work/
tools/parcels/work-receive.log
tools/route-maps/venv/
tools/route-maps/__pycache__/

# original route-map files (Publisher)
/*.pub
