// Copy this to Config.js (same folder) and fill it in. Config.js is git-ignored
// because the repo is public; clasp still pushes it to Google.
// (This example is .txt so clasp does not push it as a second CONFIG.)
var CONFIG = {
  SHEET_ID: "",              // the sync spreadsheet's ID
  MASTER_FILE_ID: "",        // the master CSV's Drive file ID
  COMPANY: "",
  OPS_PHONE: "",
  PHOTO_FOLDER: "Lukez Bunz Reports",
  ESCALATE_MINUTES: 15,
  EMAIL: {
    "default": [],           // used for any role whose own list is empty
    lead: [],
    ops: [],
    mechanic: [],
    binmaint: []
  }
};
